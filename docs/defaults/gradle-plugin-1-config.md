### Java and Kotlin configuration for Gradle plugins

- Configures the Java compilation to use the `java-target` version.
- Hardcodes the Kotlin API and language level to the [latest supported by Gradle](https://docs.gradle.org/current/userguide/compatibility.html#kotlin).
- Configures the Kotlin compiler to use classes for SAM conversion and lambdas.
